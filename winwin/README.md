# winwin

ウィンドウを決まった位置・大きさに配置する Windows 用アプリです。

- 任意のショートカットキー押下でウィンドウをリサイズ・再配置
- 1 つのショートカットに複数の配置を割り当てるサイクルショートカットに対応

<img width="714" height="579" alt="image" src="https://github.com/user-attachments/assets/ae84544f-b325-4c9a-8de3-e9f0df4f2191" />

## 動作環境

- Windows 10 (1809 以降) / Windows 11 (x64 のみ)

## インストール

[Releases](https://github.com/y-saeki/vibe-applications/releases) の `winwin-v<version>` から
`winwin_<version>_x64-setup.exe` をダウンロードして実行し、インストーラの案内に従ってください。
管理者権限は不要です。

### Windows で「WindowsによってPCが保護されました」と表示される場合

コード署名を行っていないため、ダウンロードしたインストーラは SmartScreen に警告されます。  
「詳細情報」をクリックすると「実行」ボタンが出るので、押すと続けられます。

> [!CAUTION]
> この操作に不安を感じる場合、無理に実行せず、本アプリケーションの利用を避けてください。

## 使い方

タスクトレイアイコンから設定画面が起動できます。  
設定画面で登録したショートカットを押下すると、アクティブなウィンドウが設定したサイズ・位置にリサイズされます。

### 配置の追加・変更

配置は「基準位置」「幅・高さ」の組み合わせで決まります。

- 基準位置: ウィンドウを画面のどこに寄せるか
- 幅・高さ: 画面に対する比率。`1/2` で半分、`1` で画面いっぱい

### サイクルショートカット

同じショートカットに複数の配置を設定すると、一覧の上にあるものから順に切り替わります。  
たとえば `Ctrl + Shift + ←` に対して「左半分」「左2/3」「左5/6」の設定を作成すると、 `Ctrl + Shift` を押したまま `←` を押すたびにウィンドウが 左半分 → 左2/3 → 左5/6 → 左半分 → … と順番に切り替わります。

### 終了する

通知領域のアイコンを右クリックし、「終了」を選びます。

### 更新・アンインストール

更新するときは、新しいバージョンのインストーラを実行してください。起動中の winwin は自動で終了し、上書きされます。  
設定はそのまま引き継がれます。

アンインストールは、Windows の「設定」→「アプリ」→「インストールされているアプリ」から winwin を選んで行います。  
途中でショートカットの設定も削除するかを聞かれます。

## 制限事項

- 管理者として実行されているウィンドウは、winwin も管理者として実行しているときにしか動かせません
- Windows や他のアプリケーションがすでに使っているショートカット(`Win + ←` など)は登録できません。登録できなかったものは起動時に通知でお知らせします
- アプリケーションによっては、最小サイズなどの都合で指定どおりの大きさにならないことがあります
- 自動アップデートはありません。更新するときは新しいインストーラを実行してください
- 配布物はコード署名をしていません

## 開発者向け情報

ビルド方法、ディレクトリ構成、リリースの手順などは [DEVELOPMENT.md](./DEVELOPMENT.md) にあります。
